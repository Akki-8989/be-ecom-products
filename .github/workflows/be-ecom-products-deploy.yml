name: Deploy

on:
  workflow_dispatch:

jobs:
  deploy:
    uses: Akki-8989/Infrastructure/.github/workflows/deploy-be-ecom-products.yml@main
    with:
      repo_name: be-ecom-products
      infra_repo: Infrastructure
    secrets: inherit
