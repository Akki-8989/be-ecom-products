name: Deploy

on:
  workflow_dispatch:

jobs:
  deploy:
    uses: Akki-8989/infra-repo/.github/workflows/deploy-be-ecom-products.yml@main
    with:
      repo_name: be-ecom-products
      infra_repo: infra-repo
    secrets: inherit
